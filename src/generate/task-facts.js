'use strict';
function extract(markdown){return {title:(/^# (.+)$/m.exec(markdown)||[])[1]||null,stepIds:[...markdown.matchAll(/<!-- step:([^ ]+) -->/g)].map(m=>m[1]),images:[...markdown.matchAll(/!\[[^\]]*\]\(([^)]+)\)/g)].map(m=>m[1]),uiTexts:[...markdown.matchAll(/「([^」]+)」/g)].map(m=>m[1]),verified:/已验证结果：/.test(markdown),expected:/预期结果：/.test(markdown)}}
function validateTaskFinal(markdown,facts){const got=extract(markdown);const errors=[];if(got.title!==facts.title)errors.push('一级标题与任务标题不一致。');if(JSON.stringify(got.stepIds)!==JSON.stringify(facts.stepIds))errors.push('step.id 或步骤顺序发生变化。');if(JSON.stringify(got.images)!==JSON.stringify(facts.images))errors.push('截图引用发生变化。');for(const image of got.images)if(!image.replace(/\\/g,'/').includes('/images/annotated/'))errors.push('正式文档只能引用 annotated 图片。');if(JSON.stringify(got.uiTexts)!==JSON.stringify(facts.uiTexts))errors.push('已确认的 UI 原文发生变化。');if(facts.completionVerification==='expected'&&got.verified)errors.push('未执行写操作时不能声称结果已验证。');if(facts.completionVerification==='verified'&&!got.verified)errors.push('完成标志丢失 verified 语义。');return errors.length?{ok:false,errors}:{ok:true}}
module.exports={extract,validateTaskFinal};
