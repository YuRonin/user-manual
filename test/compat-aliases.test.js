'use strict';
const assert=require('assert'),fs=require('fs'),os=require('os'),path=require('path'),{spawnSync}=require('child_process');
const CLI=path.resolve(__dirname,'..','bin','install-compat.js');const root=fs.mkdtempSync(path.join(os.tmpdir(),'manual-compat-'));
try{const codex=path.join(root,'codex'),claude=path.join(root,'claude');const r=spawnSync(process.execPath,[CLI,'--codex-home',codex,'--claude-home',claude,'--json'],{encoding:'utf8'});assert.strictEqual(r.status,0,r.stderr);const out=JSON.parse(r.stdout);for(const name of ['manual-init','manual-inspect','manual-capture','manual-generate','manual-discover-tasks','manual-verify']){const skill=path.join(codex,'skills',name,'SKILL.md'),command=path.join(claude,'commands',`${name}.md`);assert.ok(fs.existsSync(skill),skill);assert.ok(fs.existsSync(command),command);assert.match(fs.readFileSync(skill,'utf8'),new RegExp(`name: ${name}`));assert.match(fs.readFileSync(command,'utf8'),new RegExp(`manual\\.js\" ${name.replace(/^manual-/,'')}`))}assert.ok(out.installed.length>=12);process.stdout.write('\ncompat aliases\n  ✓ Codex $manual-* 与 Claude /manual-* 别名安装成功\n\n1 passed, 0 failed\n')}finally{fs.rmSync(root,{recursive:true,force:true})}
